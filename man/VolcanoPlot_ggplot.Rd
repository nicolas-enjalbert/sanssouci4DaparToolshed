% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcanoPlot.R
\name{VolcanoPlot_ggplot}
\alias{VolcanoPlot_ggplot}
\title{Volcano plot}
\usage{
VolcanoPlot_ggplot(
  pval,
  logFC,
  sanssouci_object,
  th_pval = 1,
  th_qval = 1,
  th_logfc = 0,
  conditions = NULL,
  pal = NULL
)
}
\arguments{
\item{pval}{A \code{numeric} vector of p-value values.
This vector must have the same length as \code{logFC}.}

\item{logFC}{A \code{numeric} vector of log(fold change) values.
This vector must have the same length as \code{pval}.}

\item{sanssouci_object}{A \code{SansSouci} object. Must be calibrated using the
function \code{fit}.}

\item{th_pval}{A \code{numeric(1)} which is the p-value threshold under which
proteins are selected.}

\item{th_qval}{A \code{numeric(1)} which is the q-value threshold under which
proteins are selected.}

\item{th_logfc}{A \code{numeric(1)} which is the absolute log(fold change)
threshold above which proteins are selected.}

\item{conditions}{A \code{character(2)} containing the names of both conditions.}

\item{pal}{A \code{character(2)} containing the colors used for differential and
non differential proteins.}
}
\value{
A \code{ggplot2} object providing a volcano plot.
}
\description{
Create a volcano plot
}
\examples{
#xxx a faire
NULL

}
\author{
Manon Gaudin
}
