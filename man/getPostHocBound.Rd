% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export2dapartoolshed.R
\name{getPostHocBound}
\alias{getPostHocBound}
\title{Get info of post hoc selection}
\usage{
getPostHocBound(object, selection_name)
}
\arguments{
\item{object}{QFeatures object containing post hoc selection}

\item{selection_name}{character, name of the selection}
}
\value{
a list containing:
\itemize{
\item pval_thr: the threshold on pvalues used for the selection,
\item logfc_thr: the threshold on log Foldchange used for the selection,
\item n_sel: the number of selected proteins,
\item FDP: the post hoc bound on the FDP,
\item TP: the post hoc bound on the TP
}
}
\description{
Get info of post hoc selection
}
\examples{
numdata <- data.frame(
  S1 = c(2, 5, 9, 10, 7, 8, 6, 8, 6, 7),
  S2 = c(7, 8, 5, 5, 7, 9, 9, 4, 9, 1),
  S3 = c(4, 1, 4, 9, 6, 8, 5, 3, 4, 3),
  S4 = c(9, 8, 9, 8, 3, 6, 4, 1, 4, 5),
  names = paste0("Prot", seq_len(10))
)
obj <- QFeatures::readQFeatures(numdata,
  quantCols = seq_len(4),
  fnames = "names",
  name = "datatest"
)

coldata <- data.frame(
  quantCols = c("S1", "S2", "S3", "S4"),
  Condition = c("C1", "C1", "C2", "C2"),
  Bio.Rep = as.character(seq_len(4))
)
rownames(coldata) <- coldata$quantCols
SummarizedExperiment::colData(obj) <- coldata

ss_obj <- SansSouci4DT(obj)
ss_obj <- sanssouci::fit(ss_obj, alpha = 0.5, B = 0)
obj <- export2dapartoolshed(ss_obj, 0.1, 0.5, obj)
getPostHocBound(obj, "sel_1")
}
\author{
Nicolas Enjalbert-Courrech and Pierre Neuvial
}
