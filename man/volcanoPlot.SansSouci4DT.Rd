% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcanoPlot.R
\name{volcanoPlot.SansSouci4DT}
\alias{volcanoPlot.SansSouci4DT}
\title{Volcano plot}
\usage{
\method{volcanoPlot}{SansSouci4DT}(
  x,
  pval_thr = 1,
  qval_thr = 1,
  logfc_thr = 0,
  conditions = NULL,
  pal = NULL,
  interactive = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{SansSouci4DT} object. Must be calibrated using the
function \code{sanssouci::fit}.}

\item{pval_thr}{A \code{numeric(1)} which is the p-value threshold under which
proteins are selected.}

\item{qval_thr}{A \code{numeric(1)} which is the q-value threshold under which
proteins are selected.}

\item{logfc_thr}{A \code{numeric(1)} which is the absolute log(fold change)
threshold above which proteins are selected.}

\item{conditions}{A \code{character(2)} containing the names of both conditions.}

\item{pal}{A \code{character(2)} containing the colors used for non-differential
and differential proteins.}

\item{interactive}{A \code{logical(1)} defining if the volcano plot should be
interactive and use plotly (TRUE) or not and use ggplot2 (FALSE).}

\item{...}{unused}
}
\value{
If \code{interactive} is TRUE, a \code{plotly} object providing a volcano
plot. Else, a \code{ggplot2} object providing a volcano plot.
}
\description{
Create a volcano plot from a \code{SansSouci4DT} object. This volcano plot is made
with \code{ggplot2} and indicates the number of selected proteins with the given
thresholds, as well as the minimal number of true positives.
}
\examples{
library(sanssouci)
n <- 20
p <- 32
X <- as.data.frame(matrix(rnorm(n * p), ncol = n))
colnames(X) <- paste0("S", seq_len(n))
X[,"names"] <- paste0("Prot", seq_len(p))
group <- rep(c(1, 0), length.out = n)
obj <- QFeatures::readQFeatures(X,
  quantCols = seq_len(n),
  fnames = "names",
  name = "datatest"
)

coldata <- data.frame(
  quantCols = paste0("S", seq_len(n)),
  Condition = group,
  Bio.Rep = as.character(seq_len(n))
)
rownames(coldata) <- coldata$quantCols
SummarizedExperiment::colData(obj) <- coldata

ss_obj <- SansSouci4DT(obj)
ss_obj <- sanssouci::fit(ss_obj, alpha = 0.5, B = 100)

volcanoPlot(ss_obj, pval_thr = 0.5, logfc_thr = 0.05)
\dontrun{
volcanoPlot(ss_obj, pval_thr = 0.5, logfc_thr = 0.05, interactive = TRUE)
}

}
\author{
Manon Gaudin, Nicolas Enjalbert Courrech
}
